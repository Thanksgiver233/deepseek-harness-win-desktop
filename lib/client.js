// @deepseek-ai/dsh-win-desktop — client bundle (ESM stub)
// The full React bundle is produced by tsdown. This stub enables basic operation.
export function apply(ctx) {
  console.log('[win-desktop] Client loaded (stub mode)')
}
export default { apply }
