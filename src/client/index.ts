/**
 * @deepseek-ai/dsh-win-desktop/client — Windows Desktop panel plugin, browser half.
 *
 * Host side is a no-op; the React UI is injected via slot registration.
 */

/** Empty host-side apply — plugin row exists for composition but has no host behavior. */
export function apply(): void {}
